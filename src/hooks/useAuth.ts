// Re-exported from AuthContext for backwards compatibility.
// All state and logic lives in src/context/AuthContext.tsx
export { useAuth } from '@/context/AuthContext'
export type { UserRole } from '@/context/AuthContext'
