/**
 * Demo mode: when set to an array of numbers, the entire app filters data
 * to only show information belonging to those desarrolloids.
 * Set to null to show all data.
 */
export const DEMO_DESARROLLOIDS: number[] | null = [11, 20]
